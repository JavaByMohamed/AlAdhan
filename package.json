{
  "name": "aladhan",
  "version": "1.0.0",
  "private": true,
  "description": "AlAdhan prayer times web app",
  "scripts": {
    "start": "sh -c 'pids=$(lsof -ti tcp:3000 2>/dev/null); if [ -n \"$pids\" ]; then kill $pids; fi; python3 -m http.server 3000'",
    "dev": "npm start"
  }
}
